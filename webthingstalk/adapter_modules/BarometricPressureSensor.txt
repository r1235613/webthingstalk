import json
import requests

api_url = '{api_url}'
device_name = '{device_name}'
device_model = 'WT_BarometricPressureSensor'

idf_list = ['wtBarometricPressure-I']
odf_list = []

def wtBarometricPressure_I():
    r = requests.get('{webthing_url}/properties/{barometric_pressure_property_name}', headers={headers})
    return json.loads(r.text).get('{barometric_pressure_property_name}', 'None')
