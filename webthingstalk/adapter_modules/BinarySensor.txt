import json
import requests

api_url = '{api_url}'
device_name = '{device_name}'
device_model = 'WT_BinarySensor'

idf_list = ['wtBoolean-I']
odf_list = []

def wtBoolean_I():
    r = requests.get('{webthing_url}/properties/{boolean_property_name}', headers={headers})
    return json.loads(r.text).get('{boolean_property_name}', 'None')
