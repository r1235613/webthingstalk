import json
import requests

api_url = '{api_url}'
device_name = '{device_name}'
device_model = 'WT_Camera'

idf_list = ['wtImage-I']
odf_list = []

def wtImage_I():
    r = requests.get('{webthing_url}/properties/{image_property_name}', headers={headers})
    return json.loads(r.text).get('{image_property_name}', 'None')
