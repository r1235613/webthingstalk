import json
import requests

api_url = '{api_url}'
device_name = '{device_name}'
device_model = 'WT_AirQualitySensor'

idf_list = ['wtConcentration-I', 'wtDensity-I']
odf_list = []

def wtConcentration_I():
    r = requests.get('{webthing_url}/properties/{concentration_property_name}', headers={headers})
    return json.loads(r.text).get('{concentration_property_name}', 'None')

def wtDensity_I():
    r = requests.get('{webthing_url}/properties/{density_property_name}', headers={headers})
    return json.loads(r.text).get('{density_property_name}', 'None')
