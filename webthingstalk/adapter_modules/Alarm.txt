import json
import requests

api_url = '{api_url}'
device_name = '{device_name}'
device_model = 'WT_Alarm'

idf_list = ['wtAlarm-I']
odf_list = []

def wtAlarm_I():
    r = requests.get('{webthing_url}/properties/{alarm_property_name}', headers={headers})
    return json.loads(r.text).get('{alarm_property_name}', 'None')
